server:
  port: 9001

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zclink/springcloudconfigdemo.git
          default-label:
          search-paths: master
#          username:
#          password: